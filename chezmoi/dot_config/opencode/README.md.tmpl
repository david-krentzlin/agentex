# OpenCode Config

This target is reserved for the agent-specific OpenCode configuration.

The full OpenCode config has not been migrated into the new `chezmoi` source state yet.
For now, this file is a marker that the `agent` target owns `~/.config/opencode`.
