#!/usr/bin/env bash
set -euo pipefail

if [[ $# -gt 0 ]]; then
	git log --format='%ad' --date=format:'%Y-%m' "$@" | sort | uniq -c
	exit 0
fi

git log --format='%ad' --date=format:'%Y-%m' | sort | uniq -c
