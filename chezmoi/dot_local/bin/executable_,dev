#!/usr/bin/env bash
set -euo pipefail

if [[ $# -eq 0 ]]; then
	exec limactl shell --tty --reconnect --workdir /home/dev --shell /usr/bin/zsh dev
fi

exec limactl shell --tty --reconnect --workdir /home/dev --shell /usr/bin/zsh dev "$@"
